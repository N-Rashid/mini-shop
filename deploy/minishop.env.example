# Copy to minishop.env on the server and set a unique secret:
#   cp deploy/minishop.env.example deploy/minishop.env
#   nano deploy/minishop.env
#
# Generate secret: python3 -c "import secrets; print(secrets.token_hex(32))"

MINISHOP_PRODUCTION=1
MINISHOP_SECRET_KEY=replace-with-random-secret
